---
import { Image } from 'astro:assets';
import FormattedDate from './FormattedDate.astro';
import { formatReadingTime } from '../utils/reading-time';

interface Props {
  title: string;
  description: string;
  pubDate: Date;
  heroImage?: ImageMetadata;
  tags?: string[];
  readingTime: number;
  slug: string;
}

const { title, description, pubDate, heroImage, tags = [], readingTime, slug } = Astro.props;
---

<article class="group rounded-2xl bg-white shadow-sm ring-1 ring-secondary-200 transition-all duration-300 hover:shadow-lg hover:ring-primary-300 dark:bg-secondary-900 dark:ring-secondary-800 dark:hover:ring-primary-700 overflow-hidden">
  {heroImage && (
    <a href={`/blog/${slug}/`} class="block overflow-hidden">
      <Image
        width={720}
        height={360}
        src={heroImage}
        alt=""
        class="aspect-video w-full object-cover transition-transform duration-300 group-hover:scale-105"
      />
    </a>
  )}
  <div class="p-5 sm:p-6">
    <h2 class="text-lg font-semibold leading-snug tracking-tight text-secondary-900 dark:text-secondary-100">
      <a
        href={`/blog/${slug}/`}
        class="transition-colors duration-200 hover:text-primary-600 dark:hover:text-primary-400"
      >
        {title}
      </a>
    </h2>
    <p class="mt-2 text-sm leading-relaxed text-secondary-600 dark:text-secondary-400">
      {description}
    </p>
    <div class="mt-4 flex flex-wrap items-center justify-between gap-2 text-xs text-secondary-500 dark:text-secondary-400">
      <div class="flex items-center gap-1.5">
        <FormattedDate date={pubDate} />
        <span aria-hidden="true">&middot;</span>
        <span>{formatReadingTime(readingTime)}</span>
      </div>
      {tags.length > 0 && (
        <div class="flex flex-wrap gap-1.5">
          {tags.map((tag) => (
            <span class="rounded-full bg-primary-100 px-2 py-0.5 text-xs font-medium text-primary-800 dark:bg-primary-900 dark:text-primary-200">
              {tag}
            </span>
          ))}
        </div>
      )}
    </div>
  </div>
</article>
